{
  "name": "finagentx",
  "version": "1.0.0",
  "private": true,
  "scripts": {
    "build": "echo 'Static build — nothing to compile'",
    "start": "npx serve public"
  },
  "devDependencies": {
    "serve": "^14.2.3"
  }
}
